# CHANGE <TENANT_NAME> TO MATCH YOUR TENANT NAME!
#
# This environment file is loaded during docker-compose `build`, `up`, and `down`.
# Variables defined here are only used to expand docker-compose placeholders and not passed into the containers' environment.
ANTITHESIS_REGISTRY=us-central1-docker.pkg.dev/molten-verve-216720/<TENANT_NAME>-repository
TAG=latest